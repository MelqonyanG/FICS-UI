import React from "react";

class AviableGame extends React.Component {
    render() {
        return (
          <div className="container">
          <h1>{this.props.msg}</h1>
        </div>
      )}
}

export default AviableGame;
